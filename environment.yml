name: transgrokking-local
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.2
  - pytorch-cuda=12.1
  - numpy=1.26.4
  - pyyaml=6
  - pip
  - pip:
      - "pytest>=9,<10"
      - "ruff>=0.9,<1"
