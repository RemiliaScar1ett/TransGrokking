# Historical prototype

`main.py` and `data.py` are preserved verbatim from Git commit `25baac9`.
They are historical references, not supported entry points. The prototype enables
TF32/BF16 autocast and congruence loss, so it does not satisfy the M0 CE-only FP32
protocol and was not executed during the refactor.

